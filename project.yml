name: Smena
options:
  bundleIdPrefix: com.smena
  deploymentTarget:
    iOS: "16.0"
packages: {}
targets:
  Smena:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - path: Smena
    settings:
      base:
        INFOPLIST_FILE: Smena/Resources/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.smena.tracker
        IPHONEOS_DEPLOYMENT_TARGET: "16.0"
        CURRENT_PROJECT_VERSION: 1
        MARKETING_VERSION: 1.0
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ENABLE_BITCODE: NO
    dependencies: []
    scheme:
      testTargets: []
